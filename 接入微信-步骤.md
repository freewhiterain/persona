# 接入微信（WeChatPadPro）— 操作清单

> 前提：Docker Desktop 已启动；本项目在 `D:\Desktop\python\c`。
> 详细说明见 [docs/wechatpadpro.md](docs/wechatpadpro.md)。
> ⚠️ 用**副微信号**，个人号自动化有封号风险。

## 一、起 WeChatPadPro 服务

1. `git clone https://github.com/WeChatPadPro/WeChatPadPro.git`，进入 `WeChatPadPro/deploy`
2. 改 `deploy/.env`：设一个 `ADMIN_KEY=自己定一个`
3. 改 `deploy/docker-compose.yml`（Windows 端口保留段问题）：
   - `wechatpadpro` 的端口 `"${PORT:-1238}:1238"` → 改成 `"12380:1238"`
   - 删掉 `mysql` 的 `- "${MYSQL_PORT:-3306}:3306"` 这行
   - 删掉 `redis` 的 `- "${REDIS_PORT:-6379}:6379"` 这行
4. `docker compose up -d`，然后 `docker compose ps` 看三个容器都 `healthy`

## 二、登录副号、拿 authKey

5. 浏览器打开 `http://localhost:12380/docs`（Swagger UI）
6. `POST /admin/GenAuthKey1?key=<第2步的ADMIN_KEY>`，body：`{"Count":1,"Days":365}`
   → 返回里的 `authKeys[0]` 就是 **authKey**
7. `POST /login/GetLoginQrCodeNewX?key=<authKey>`，body：`{"Check":false,"Proxy":""}`
   → 返回里 `qrCodeBase64` 是二维码图片，用**副微信号扫码登录**（有效期约 290 秒，过期重拿）
8. `GET /login/GetLoginStatus?key=<authKey>` 确认在线；从返回里记下这个号的 **wxid**
   （首次登录 24 小时内可能自动掉线一次，重新扫一遍）

## 三、配 persona 并启动

9. 编辑 `D:\Desktop\python\c\.env`，加一行：
   ```
   WECHATPADPRO_TOKEN=<第6步的authKey>
   ```
10. 编辑 `D:\Desktop\python\c\config.toml` 的 `[wechatpadpro]` 段：
    ```toml
    enabled   = true
    base_url  = "http://localhost:12380"
    self_wxid = "<第8步的wxid>"
    character = "lin"
    push_mode = "ws"
    ```
11. `cd D:\Desktop\python\c && uv sync --extra wechat`
12. `uv run persona init`（只需一次）
13. `uv run persona wechat`

## 四、验证

14. 用**另一个**微信号给副号发一条消息
15. 看副号是否自动回复；不对就把 `uv run persona wechat` 的终端日志贴出来

---

## 还没做的（骨架限制）

- 图片 / 语音收发（要接 STT / TTS / 视觉）
- 群聊（会被自动跳过）
- WS 消息帧的字段大小写未用真实登录验证过——adapter 已兼容多种写法，真收到不对再调
- 掉线自动重登、多账号
